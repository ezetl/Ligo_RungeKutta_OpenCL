TODO:
- Encontrar otra forma de hacer switch entre float64 y float32
- Cambiar nombres de variables por nombres de mas de una letra (D'oh).
- implementar el rk_step, el f_rhs
- Ver como mandar muchas condiciones iniciales en un solo batch, no solamente una
- La variable error deberia ser un array. Ver si conviene eso o ver como pasar punteros en todo caso

Ideas:
- tolerance for solver: probar con otros numeros a ver que da (con max time step tambien)
- para generar las condiciones iniciales, hacer un yield de la condicion generada. De esa forma se 
  podria iterar sobre las condiciones iniciales como:
        for cond in generate_init_cond():
            etc etc

Preguntas:
- que se guarda en el vector 'ya'? es la evolucion de una particula en distintas etapas? siempre da lo mismo en todos los slots
    (RTA: Es una prueba, solo importan las 10 variables nomas).
- Los aij, bij, conviene crearlos en memoria global o memoria local a cada kernel? repeticion de datos vs. eficiencia ??
